# docker/kafka/Dockerfile
# NOTE: This Dockerfile is not used by docker-compose.
# Kafka is pulled directly as apache/kafka:3.9.0 in docker-compose.yml.
# Kept here only if you need a custom Kafka build in the future.
FROM apache/kafka:3.9.0